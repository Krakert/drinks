import 'package:drinks/core/usecases/usecase.dart';
import 'package:either_dart/either.dart';

class GetListLocationsUseCase implements UseCase<Either, dynamic> {
  @override
  Future<Either> call({params}) {
    throw UnimplementedError;
  }
}
